package edu.gatech.donatrix.view;

public interface Display {

    //Dispalys a toast containing the error message
    public displayException(Exception e) {
        switch (e) {
            //Write cases for exceptions and write the Toast code here, not in the activity
        }
    }
}